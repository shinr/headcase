1. level creates an entity based on data
2. entity generates it's own vertices and element data
3. level queues an entity to the renderer, renderer saves the vertice data
and returns an index offset which is then saved to the entity
4. when all entities are loaded renderer generates buffers
5. when rendering, queue is generated by shader, in queue all entites
with the shader are queued element count and their offset
6. render